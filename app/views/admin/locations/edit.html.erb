
<% content_for :top_nav do %>
	<% if can? :read, @location %>
		<li><%= link_to content_tag(:i,"",:class => "icon-map-marker") + "View Location", admin_location_path(@location), :class => "" %></li>
	<% end %>
<% end %>

<h1 class="page-title"><i class="icon-edit"></i>&nbsp;Editing:&nbsp;<span><%= @location.name %></span></h1>

<%= render 'form' %>

<%= link_to 'Show', admin_location_path %> |
<%= link_to 'Back', admin_locations_path %>
